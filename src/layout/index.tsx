import { Layout } from "antd"
import LayoutHeader from "./layout-header"
const WebContainer = () => {
  return (
    <Layout>
      <LayoutHeader />
    </Layout>
  )
}

export default WebContainer